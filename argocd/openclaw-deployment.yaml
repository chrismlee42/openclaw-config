apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: openclaw
  name: openclaw
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: openclaw
  template:
    metadata:
      labels:
        app: openclaw
    spec:
      serviceAccountName: openclaw
      initContainers:
        - name: init-config
          image: "ghcr.io/openclaw/openclaw:2026.2.15"
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              mkdir -p /home/openclaw/.openclaw
              if [ "merge" = "merge" ] && [ -f /home/openclaw/.openclaw/openclaw.json ]; then
                # Merge: existing config as base, helm config overwrites
                node -e "
                  const fs = require('fs');
                  const existing = JSON.parse(fs.readFileSync('/home/openclaw/.openclaw/openclaw.json', 'utf8'));
                  const helm = JSON.parse(fs.readFileSync('/config/openclaw.json', 'utf8'));
                  const deepMerge = (target, source) => {
                    for (const key of Object.keys(source)) {
                      if (source[key] && typeof source[key] === 'object' && !Array.isArray(source[key])) {
                        target[key] = target[key] || {};
                        deepMerge(target[key], source[key]);
                      } else {
                        target[key] = source[key];
                      }
                    }
                    return target;
                  };
                  const merged = deepMerge(existing, helm);
                  fs.writeFileSync('/home/openclaw/.openclaw/openclaw.json', JSON.stringify(merged, null, 2));
                "
              else
                cp /config/openclaw.json /home/openclaw/.openclaw/openclaw.json
              fi
              chown -R 1000:1000 /home/openclaw/.openclaw
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
          volumeMounts:
            - name: data
              mountPath: /home/openclaw/.openclaw
            - name: config
              mountPath: /config
              readOnly: true
      containers:
        - name: openclaw
          image: "ghcr.io/openclaw/openclaw:2026.2.15"
          imagePullPolicy: IfNotPresent
          command:
            - node
            - dist/index.js
            - gateway
            - --bind
            - lan
            - --port
            - "18789"
            - --allow-unconfigured
          ports:
            - name: gateway
              containerPort: 18789
              protocol: TCP
            - name: canvas
              containerPort: 18793
              protocol: TCP
          envFrom:
            - secretRef:
                name: openclaw
          env:
            - name: HOME
              value: /home/openclaw
            - name: TZ
              value: "America/Chicago"
            - name: PATH
              value: "/home/linuxbrew/.linuxbrew/bin:/home/linuxbrew/.linuxbrew/sbin:/root/.bun/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - cp /home/openclaw/.config/gogcli/secret/google-credentials.json /home/openclaw/.config/gogcli/credentials.json
          livenessProbe:
            tcpSocket:
              port: gateway
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            tcpSocket:
              port: gateway
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          startupProbe:
            tcpSocket:
              port: gateway
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
            successThreshold: 1
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 2Gi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: data
              mountPath: /home/openclaw/.openclaw
            - name: google-credentials
              mountPath: /home/openclaw/.config/gogcli/secret
              readOnly: true
            - name: homebrew
              mountPath: /home/linuxbrew/.linuxbrew
        - name: chromium
          image: "zenika/alpine-chrome:124"
          imagePullPolicy: IfNotPresent
          command:
            - chromium-browser
            - --headless
            - --disable-gpu
            - --disable-software-rasterizer
            - --disable-dev-shm-usage
            - --no-sandbox
            - --remote-debugging-address=0.0.0.0
            - --remote-debugging-port=9222
          ports:
            - name: cdp
              containerPort: 9222
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
        - name: homebrew
          image: "homebrew/brew:latest"
          imagePullPolicy: IfNotPresent
          command:
            - tail
            - -f
            - /dev/null
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: homebrew
              mountPath: /home/linuxbrew/.linuxbrew
      volumes:
        - name: config
          configMap:
            name: openclaw
        - name: data
          persistentVolumeClaim:
            claimName: openclaw
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi
        - name: google-credentials
          secret:
            secretName: google-credentials
        - name: homebrew
          persistentVolumeClaim:
            claimName: homebrew
